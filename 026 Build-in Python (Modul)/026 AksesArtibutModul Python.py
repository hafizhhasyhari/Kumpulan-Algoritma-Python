# Mengakses atribut pada suatu modul

import sys          # Memanggil modul built-in sys

a = sys.maxsize          # Mengakses atribut maxsize pada modul sys
b = 2**63 - 1
print("Nilai a = ",a)
print("Nilai b = ",b)
